const SearchInput = () => {
  return (
    <input
      id="campoPesquisa"
      type="text"
      placeholder="Pesquisar"
      className="barra-pesquisa-input"
      autoComplete="off"
    />
  );
};

export default SearchInput;
